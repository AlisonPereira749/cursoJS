// For clássico - Geralmente com interáveis (arrays ou strings)
// For In - Retorna o índice ou chaves (arrays, strings ou objetos)
// For Of - Retorna o valor em si (iteráveis, arrays ou strings)

// For Of Lê o valor de cada índice 

const nomes = ['Luiz', 'Paulo', 'Henrique', 'João']
for (let valor of nomes) {
    console.log(valor)
}